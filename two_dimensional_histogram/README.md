# **2D Histogram Visualization of Random Data**

Creates a *2D histogram* (heatmap-style) of two random normally distributed variables using `matplotlib`. Visualizes the density of point occurrences in a 2D plane with color-coded frequency.

***:)***