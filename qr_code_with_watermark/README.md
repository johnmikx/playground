# **QR Code Generator with Centered Image Watermark**

Generates a high-error-correction QR code from user input and embeds a centered image watermark (e.g., a logo) using `qrcode` and `PIL`. The result is saved as `qr_code.png`, making it suitable for custom branded QR applications.

***:)***