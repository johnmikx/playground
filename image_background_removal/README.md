# **Background Removal from Images Using Rembg**

Uses the `rembg` library and ONNX runtime to remove the background from an input image (`pic.png`) and save the transparent result. Demonstrates automated image segmentation with minimal code.

***:)***