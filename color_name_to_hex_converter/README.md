# **Convert CSS Color Name to Hex Code**

Converts a **CSS color name** (e.g., *"red"*, *"navy"*) into its corresponding hexadecimal code using the `webcolors` library. Useful for web development and design tasks requiring color format conversions.

***:)***