# **Boxplot of Random Data**

A Python script utilizing the `seaborn`, `numpy`, and `matplotlib` libraries to generate a boxplot that visualizes the distribution, spread, and potential outliers in a dataset of randomly generated values.

***:)***