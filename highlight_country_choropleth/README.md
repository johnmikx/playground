# **Interactive Country Highlight on World Map**

Uses `plotly express` to generate a *choropleth map* that highlights a single user-specified country with a color gradient. Demonstrates interactive geographic visualization with minimal input.

***:)***