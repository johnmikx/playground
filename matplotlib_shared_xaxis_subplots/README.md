# **Matplotlib Shared X-Axis Subplots**

Demonstrates creating multiple subplots in `matplotlib.pyplot` with a shared x-axis, plotting the exponential function on both plots but with different y-axis limits to highlight specific data ranges.

***:)***