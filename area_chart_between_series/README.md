# **Area Chart Between Two Data Series**

Uses `matplotlib` to plot an area chart showing the filled region between two data series (`y1` and `y2`). Reinforces visualizing the difference between datasets over a common x-axis.

***:)***